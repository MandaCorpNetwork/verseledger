{
  "name": "verseledger",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -p \"[{name}]\" -n \"CLIENT,SERVER\" -c \"bgGreen.bold,bgBlue.bold\" \"manypkg run client dev\" \"manypkg run server dev\"",
    "postinstall": "preconstruct dev && manypkg check",
    "clean": "yarn manypkg exec rm -rf node_modules && manypkg exec rm -rf dist && rm -rf node_modules",
    "test": "yarn test:server && yarn test:client",
    "lint": "yarn manypkg run server lint && yarn manypkg run vl-shared lint && manypkg run client lint",
    "lint:check": "yarn manypkg run server lint:check && yarn manypkg run vl-shared lint:check && manypkg run client lint:check",
    "test:client": "yarn manypkg run client test",
    "test:server": "yarn manypkg run server test",
    "resetdb": "cd scripts && setup-local-db.bat",
    "migrate": "./.flyway/flyway -configFiles=./flyway.user.toml -environment=vl-db migrate",
    "cypress:develop": "yarn manypkg run client cypress open --browser chrome",
    "cypress": "yarn manypkg run client cypress run --browser chrome",
    "build": "yarn manypkg run client build",
    "start": "yarn migrate && concurrently -p \"[{name}]\" -n \"CLIENT,SERVER\" -c \"bgGreen.bold,bgBlue.bold\" \"manypkg run client serve\" \"manypkg run server serve\""
  },
  "dependencies": {
    "@babel/core": "7.25.2",
    "@babel/preset-env": "7.25.4",
    "@babel/preset-typescript": "7.24.7",
    "@changesets/cli": "2.27.8",
    "@manypkg/cli": "0.21.4",
    "@preconstruct/cli": "2.8.7",
    "concurrently": "9.0.1",
    "flywaydb-cli": "0.9.0"
  },
  "preconstruct": {
    "packages": [
      "packages/*"
    ]
  },
  "resolutions": {
    "@types/mime": "3.0.4"
  }
}
