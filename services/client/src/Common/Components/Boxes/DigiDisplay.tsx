import { Box, styled } from '@mui/material';

const DigiDisplay = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  backgroundColor: 'rgba(14,49,141,.25)',
  borderRadius: '10px',
});

export default DigiDisplay;
