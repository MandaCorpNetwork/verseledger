import { styled, Tab, Tabs } from '@mui/material';

export const SmallTabsHolo = styled(Tabs)({
  minHeight: '10px',
});

export const SmallTabHolo = styled(Tab)({
  minHeight: '10px',
  height: '10px',
  fontSize: '.8em',
});
